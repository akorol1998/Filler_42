                FIELDS
- sign of the player
- token array of (signs' coordinates)
- weight of the covered area
- Size of the map
- size of the token

1. Put a map into an Array
2. Put a token into an array

x:__ y:__

if y_diff < 0:
    y_cord = j + y_diff * -1;
if x_dif < 0:
    x_cord = i + x_diff * -1;

then check weather these coordinates are not out of the range and are allowed to be placed,
alse record the weight
